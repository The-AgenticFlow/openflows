//! binary/src/main.rs — not used as a crate root.
//!
//! The openflows binary's crate root is `src/bin/agentflow.rs`.
//! Shared modules (nodes, state, debug, orchestration, bundled) are declared there.
