[package]
name    = "openflows"
version = "1.5.2"
edition = "2021"
license = "MIT"
# Register the repo-root README with the package so release-plz tracks edits to it
# (e.g. `docs(readme): ...` commits) as part of the `openflows` changelog. Without
# this, release-plz's package-path walk only covers `binary/`, so root README/doc
# changes are dropped from the generated CHANGELOG and the GitHub release body.
readme = "../README.md"

[package.metadata.cargo-machete]
ignored = ["provisioner"]

[[bin]]
name = "openflows"
path = "src/bin/agentflow.rs"

[[bin]]
name = "openflows-doctor"
path = "src/bin/doctor.rs"

[dependencies]
pocketflow-core = { version = "0.1.0", path = "../crates/pocketflow-core" }
config          = { version = "0.2.0", path = "../crates/config" }
agent-nexus     = { version = "0.1.0", path = "../crates/agent-nexus" }
agent-forge     = { version = "0.1.0", path = "../crates/agent-forge" }
agent-vessel    = { version = "0.1.0", path = "../crates/agent-vessel" }
agent-sentinel  = { version = "1.1.6", path = "../crates/agent-sentinel" }
agent-lore      = { version = "0.1.0", path = "../crates/agent-lore" }
provisioner    = { version = "0.1.0", path = "../crates/provisioner" }
coder-client    = { version = "0.1.0", path = "../crates/coder-client" }
openflows-harness = { version = "1.2.1", path = "../crates/openflows-harness" }
clap            = { version = "4", features = ["derive"] }

  anyhow      = { workspace = true }
  serde_json  = { workspace = true }
  tokio       = { workspace = true }
  tracing     = { workspace = true }
  tracing-subscriber = { workspace = true }
  dotenvy     = { workspace = true }
  reqwest     = { workspace = true }

[dev-dependencies]
mockito = "1"
tempfile = "3"
